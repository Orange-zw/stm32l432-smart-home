# ===============================================================
# Keil 工程 .gitignore
#
# 说明：本工程是自包含仓库，STM32 标准外设库（STM32F10x_FWLib）
# 需随仓库一起分发（Keil 的 include 路径与 LCD.uvprojx 都引用它），
# 因此不再排除。原有条目 `STM32F10x_FWLib`、`Algorithm/List`
# （后者指向不存在的目录）已移除。
#
# OBJ 目录已由根目录 .gitignore 精细控制：
# 忽略编译中间产物，但保留 LCD.hex（可烧录）与库打包脚本。
# ===============================================================

OBJ
USER/DebugConfig
USER/Listings
USER/.cache
