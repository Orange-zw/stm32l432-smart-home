# ---------------------------------------------------------------
# Keil / MDK 编译中间产物与本地工程配置
# ---------------------------------------------------------------
*.o
*.d
*.crf
*.dep
*.axf
*.htm
*.lnp
*.bak
*.iex
*.sct
*.map
*.lst
*.build_log.htm
*.scvd
*.uvguix.*
*.uvoptx
*.__i
JLinkSettings.ini
code/USER/JLinkSettings.ini

# ---------------------------------------------------------------
# OBJ 目录：忽略编译中间产物，但保留可烧录产物与库打包脚本
#
# 注意：code/.gitignore 里有一条裸 `OBJ`，会排除整个目录，
# 而 git 不会进入被排除的目录，所以 negation 规则在这里不生效。
# 这两个文件是**用 git add -f 强制纳入**的；一旦被跟踪，
# 后续对它们的修改不再受 ignore 规则影响。
# 若在别处重新克隆后需要恢复，执行：
#   git add -f code/OBJ/LCD.hex "code/OBJ/A_make_esp8266_lib(2).bat"
# ---------------------------------------------------------------
code/OBJ/*
code/USER/DebugConfig/
code/USER/Listings/
code/USER/EventRecorderStub.scvd

# ---------------------------------------------------------------
# clangd / IDE 索引缓存（约 6.3 MB，属可再生成内容）
# ---------------------------------------------------------------
.cache/
code/USER/.cache/
compile_commands.json
.vscode/
.cursor/
*.code-workspace

# ---------------------------------------------------------------
# 辅助工具二进制（源码见 code/keil2clangd-master/）
# ---------------------------------------------------------------
code/USER/keil2clangd.exe
code/keil2clangd-master/.github/

# ---------------------------------------------------------------
# PC 端工具与本机产物
# ---------------------------------------------------------------
*.exe
*.log

# ---------------------------------------------------------------
# 系统文件
# ---------------------------------------------------------------
Thumbs.db
desktop.ini
.DS_Store
